{
  "name": "screeps-fns",
  "version": "0.0.4",
  "description": "Helper functions for Screeps",
  "main": "lib/screeps-fns.js",
  "scripts": {
    "test": "jest",
    "prepublishOnly": "tsc",
    "compile": "tsc",
    "document": "typedoc --out ./docs --mode file --name Screeps-fns && echo screeps-fns.arcath.net > ./docs/CNAME",
    "lint": "tslint -p .",
    "ci": "tslint -p . && tsc && jest --coverage --coverageReporters=text-lcov | coveralls"
  },
  "keywords": [
    "Screeps"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/Arcath/screeps-fns.git"
  },
  "author": "Adam Laycock",
  "license": "MIT",
  "dependencies": {
    "@types/screeps": "^2.5.3",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "@types/node": "^10.12.18",
    "coveralls": "^3.0.2",
    "tslint": "^5.12.0",
    "typedoc": "^0.14.0"
  }
}
